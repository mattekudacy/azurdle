name: Supabase health ping

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    if: vars.SITE_DOMAIN != ''
    steps:
      - name: Ping health endpoint
        run: curl -fsS "https://${{ vars.SITE_DOMAIN }}/api/health"
